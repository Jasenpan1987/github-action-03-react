name: deploy project
on:
  issues:
    types:
      - opened
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: output github context
        run: echo "Issue triggered"
          \n Title- "${{ toJSON(github.event.issue) }}"
          \m Body- "${{ github.event.body }}"
